# Optional: point app at a local Supabase Edge Function (run with: supabase functions serve server --env-file .env.local)
# VITE_SERVER_URL=http://localhost:54321/functions/v1/server

# For local Supabase function you also need (in .env.local, not committed):
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_SERVICE_ROLE_KEY=<from supabase status>
